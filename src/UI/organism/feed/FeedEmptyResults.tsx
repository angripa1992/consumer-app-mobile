import TextElement from '@/UI/atoms/text/TextElement';

const FeedEmptyResults = () => {
	return (
		<TextElement textStyles='text-light-white text-sm mt-8 text-center'>
			There aren't any events for now
		</TextElement>
	);
};

export default FeedEmptyResults;
